# Copy to .env and set your values. Never commit .env.

# Backend API URL (after you deploy to Render)
VITE_API_URL=http://localhost:3000

# Admin login password (optional; only used if you haven't switched to JWT login yet)
VITE_ADMIN_PASSWORD=
